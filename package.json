{
  "name": "atom-themes",
  "description": "API and scraper to traverse the Atom themes database",
  "version": "0.0.1",
  "main": "./compiled/atom-themes.js",
  "scripts": {
    "autocompile": "babel --watch --plugins transform-runtime --presets es2015,stage-0 -d compiled/ src/"
  },
  "dependencies": {
    "cheerio": "^0.20.0",
    "request-promise": "^2.0.1",
    "babel-runtime": "^6.6.1"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-plugin-transform-runtime": "^6.6.0",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
